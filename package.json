{
  "name": "sortable-matching",
  "version": "1.0.0",
  "description": "Sortable code challenge, matching products and listings.",
  "main": "index.js",
  "scripts": {
    "start": "[ -e results.txt ] && rm results.txt && node index.js || node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate-docs": "./node_modules/.bin/jsdoc -c ./conf.json"
  },
  "keywords": [
    "code",
    "challenge",
    "sortable"
  ],
  "author": "Michael Perrotte",
  "license": "MIT",
  "devDependencies": {
    "jsdoc": "^3.4.0",
    "jsdoc-baseline": "https://github.com/hegemonic/baseline/tarball/master",
    "semistandard": "^8.0.0"
  },
  "dependencies": {
    "async": "^2.0.1",
    "lodash": "^4.14.1",
    "nlp_compromise": "^6.5.0",
    "winston": "^2.2.0"
  }
}
